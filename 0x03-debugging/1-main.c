#include <stdio.h>

/**
 * main - causes an infinit loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinit loop incoming :(\n");

	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("infinite loop avoide! \\o/\n");

	return (0);
}
