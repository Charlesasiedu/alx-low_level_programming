#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: this is the input string
 * Return: length of a string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
